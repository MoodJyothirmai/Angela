import random
random_number_0_to_1 = random.random()
print(random_number_0_to_1)

# random() generates the random floats range with 0.0 to 1.0
# Everytime you run will see the random floats generating between 0.0 to 1.0

random_multiply = random.random() * 10
print(random_multiply)

random_float = random.uniform(2, 6)
print(random_float)
# uniform() generates the float no between the given range